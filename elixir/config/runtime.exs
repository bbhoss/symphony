import Config

# Port and host are configured dynamically in Application.start/2
# from the WORKFLOW.md config via SymphonyElixir.Config.
# This file provides sensible defaults for when the app starts
# without WORKFLOW.md configuration (e.g., in tests).
